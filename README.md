# Document Sentiment Project

Analyzes text and documents to provide sentiment analysis

# Build

`docker build -t flaskplayground .`

# Run

`docker run -it -p 5000:5000 -v $(pwd):/home flaskplayground bash`

`python app.py`
